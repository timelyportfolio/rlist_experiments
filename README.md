play with the [`rlist` package](https://github.com/renkun-ken/rlist) from @renkun-ken.  I would be delighted if we could get `lodash` type functionality with R `lists`.

** Note: I could be doing this all wrong **
